Task 0.3 – Component cơ bản (atoms)

Mục tiêu: Tạo set component dùng lại:

Việc cần làm:

Button, Input, Textarea, Select, Badge/Tag, Avatar, Popover, Dialog, Tabs, Tooltip.

Icon wrapper (dùng lucide-react).

BE-friendly:

Mọi component nhận props thuần UI, không phụ thuộc API.

Đảm bảo có loading state cho Button.